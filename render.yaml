services:
  - type: web
    name: asem-analysis-app
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: voila app.ipynb --Voila.server_url=/ --port=$PORT --no-browser --Voila.ip=0.0.0.0 --strip_sources=True --debug
